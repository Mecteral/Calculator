<Window x:Class="CalculatorWPFApplication.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Title="ShellView" Height="150" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="30" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Row="0" Grid.Column="0" x:Name="Input" />
        <Grid Grid.Row="1" Grid.Column="0" Name="CalculationButtonIsVisible">
            <Button x:Name="OnCalculationButton" Content="Calculation" Width="100"/>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1" Name="ConversionButtonIsVisible">
            <Button x:Name="OnConversionButton" Content="Conversion"/>
        </Grid>
        <ContentControl Grid.Row="2" Grid.Column="0" x:Name="ActiveItem"/>
    </Grid>
</Window>































